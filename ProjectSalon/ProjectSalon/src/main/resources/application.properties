# ===============================
# = DATABASE CONFIGURATION =
# ===============================
spring.datasource.url=jdbc:postgresql://localhost:5432/salon
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# = JPA / HIBERNATE SETTINGS =
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (Optional) Enable SQL logging at Hibernate level
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ===============================
# = SPRING SECURITY / JWT CONFIG =
# ===============================
jwt.secret=85mJ7mUQrM/4DeJmzqVp5y4CV6JHjYJCLlzU3cpyewM=

# Enable detailed Spring Security logs (optional during dev only)
logging.level.org.springframework.security=DEBUG

# ===============================
# = SERVER SETTINGS =
# ===============================
server.port=7070